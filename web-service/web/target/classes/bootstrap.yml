server:
  port: 8903
  
spring:
  application:
    name: web

  cloud:
    config:
      uri: http://localhost:8802
    services:
      registrationMethod: direct
      
  rabbitmq:
        addresses: amqp://localhost:5672
        username: admin
        password: admin
